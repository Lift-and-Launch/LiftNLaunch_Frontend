# Browser calls same-origin /api; Vite proxies to the backend
VITE_API_URL=/api

# Default proxy target: https://liftnlaunch-backend.onrender.com
# For local backend only:
# VITE_API_PROXY=http://localhost:5000
