# Local Vite: point at the deployed API (no local backend needed)
VITE_API_URL=https://liftnlaunch-backend.onrender.com/api

# Public site URL used for canonical links, Open Graph, and sitemap absolute URLs
VITE_SITE_URL=https://www.liftandlaunch.co

# Only if you run the backend locally:
# VITE_API_URL=http://localhost:5000/api
